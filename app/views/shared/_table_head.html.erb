<%
if @links_direction == nil then
	@links_direction = {
		'error_type' => "desc",
		'loc_file' => "desc",
		'id' => "desc",
		'marking' => "desc"
	}
end
if @query == nil
	@query = {}
end
%>
<thead>
	<tr>
		<th>#</th>
		<th><%= link_to("Error Type (Subtype)", @query.merge(:order => "error_type", :order_dir => @links_direction["error_type"])) %></th>
		<th><%= link_to("File [Line]", @query.merge(:order => "loc_file", :order_dir => @links_direction["loc_file"])) %></th>
		<th><%= link_to("Marking", @query.merge(:order => "marking", :order_dir => @links_direction["marking"])) %></th>
	</tr>
</thead>
