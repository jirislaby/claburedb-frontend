<h1>Listing errors from <%= @tool.name %> <%= @tool.version %>
	<%= if @marking != nil
		    (@marking<"0")?"with false marking ": (@marking=="0") ? "with unclassified marking" : "with real marking"	
	   	end%>
</h1>
<span>
	Number of listed errors: <strong><%= @errors.count %></strong>
</span>
<div>
	<table class="errors">
	  <%= render :partial => "shared/table_head_sortable" %>
	  <%= render :partial => "shared/table_body", :object => @errors %>
	</table>
</div>
<br />